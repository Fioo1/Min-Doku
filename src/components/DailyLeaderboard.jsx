import{Trophy}from'lucide-react';import{useEffect,useState}from'react';import{supabase}from'../services/supabase';const t=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;export default function DailyLeaderboard({difficulty}){const[rows,setRows]=useState([]),date=new Date().toISOString().slice(0,10);useEffect(()=>{supabase.from('game_history').select('user_id,seconds,profiles(name)').eq('daily',true).eq('challenge_date',date).eq('difficulty',difficulty).order('seconds').then(({data})=>{const ids=new Set();setRows((data||[]).filter(x=>!ids.has(x.user_id)&&ids.add(x.user_id)).slice(0,5))})},[difficulty,date]);return <div className="daily-leaderboard"><h3><Trophy size={17}/> Ranking de hoy</h3>{rows.length?<ol>{rows.map((x,i)=><li key={x.user_id}><span>{i+1}. {x.profiles?.name||'Jugador'}</span><b>{t(x.seconds)}</b></li>)}</ol>:<p>Aún no hay tiempos. ¡Sé la primera persona!</p>}</div>}
