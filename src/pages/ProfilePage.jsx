import { Award, Clock3, Edit3, Gamepad2, Save } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../context/AuthContext'
import { useGame } from '../context/GameContext'
export default function ProfilePage(){const {user,updateProfile}=useAuth();const {player}=useGame();const [name,setName]=useState(player.name);const [editing,setEditing]=useState(false);const save=()=>{updateProfile({name:name.trim()||'Jugador'});setEditing(false)};return <section className="module-page"><div className="profile-banner"><div className="profile-avatar">{player.name[0]}</div><div><p className="eyebrow">PERFIL DE JUGADOR</p>{editing?<div className="name-edit"><input value={name} onChange={event=>setName(event.target.value)} maxLength="32"/><button onClick={save}><Save size={16}/> Guardar</button></div>:<><h1>{player.name}</h1><p>{user?.email}</p></>} </div>{!editing&&<button className="outline-button" onClick={()=>setEditing(true)}><Edit3 size={16}/> Editar perfil</button>}</div><div className="profile-stats"><article><Gamepad2/><strong>{player.solved}</strong><span>Sudokus resueltos</span></article><article><Clock3/><strong>{player.bestTime}</strong><span>Mejor tiempo</span></article><article><Award/><strong>Nivel {player.level}</strong><span>{player.xp} XP acumulada</span></article></div><div className="achievements"><p className="eyebrow">LOGROS</p><h2>Tu colección</h2><div><span>✦ Primer tablero</span><span className={player.solved>=10?'unlocked':''}>10 partidas</span><span className={player.level>=10?'unlocked':''}>Nivel 10</span><span className={player.streak>=7?'unlocked':''}>Racha de 7 días</span></div></div></section>}
